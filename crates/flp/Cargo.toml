[package]
name = "flp"
version = "0.1.0"
edition = "2024"

[dependencies]
common = {path = "../common"}
serde_json = {workspace = true}
serde = {workspace = true, features = ["derive"]}
anyhow = {workspace = true}